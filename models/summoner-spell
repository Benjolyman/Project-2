const mongoose = require('mongoose');

const summonerSpellSchema = new mongoose.Schema({
    type: String,
    description: String,
    imageUrl: String,
    required: true
});

module.exports = mongoose.model('SummonerSpell', summonerSpellsSchema);